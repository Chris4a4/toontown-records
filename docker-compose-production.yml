services:
  bot:
    build:
      context: bot/
    depends_on:
      - backend

  backend:
    build:
      context: backend/
    depends_on:
      - mongo

  mongo:
    image: mongo
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
    driver: local